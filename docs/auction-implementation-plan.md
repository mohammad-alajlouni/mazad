# Auction implementation inspection and plan

Baseline: FastAPI / SQLAlchemy / Alembic / WeasyPrint; Next.js 16 (retain, do not replace with Vite). 20 backend tests pass; frontend typecheck and production build pass before edits. Eight real generators, authenticated cookies, normalized generic items, XLS/XLSX aliases, image normalization, draft/approval and RTL/LTR already exist. Missing: auction/property schemas, selling agents/rentals, cover registry, official composition, QR/barcode, per-user ownership, usable mapping selector and auction review. Existing regeneration replaces files; approved auction snapshots must be preserved.

References inspected: all 63 guide pages as contact sheets; booklet-specific pages 9–26 enlarged; all 21 supplied booklet pages and six advertising templates; six AI PDF-compatible covers and six SVG assets; seven fonts (embedding flags 0). Guide p9 specifies A4 and Ruaq Arabic; p11 immutable Infath introduction; p14 repeat property summaries and retain missing cells as '-'; p15–20 landscape/portrait and QR variants; p21–23 conditional boundaries/information/rentals; p24 immutable type-specific terms; p25 contact; p26 configurable electronic participation. Advertising references are inputs to existing banner scope, not authorization for event collateral.

Implementation: extend projects/items with validated structured auction/property metadata; related selling-agent and rental tables; ordered images; migration 0002; ownership enforcement through authenticated session context. Keep existing item API and normalizer for both manual and Excel input. Add controlled registry/composer and HTML sections, supplied font/assets and fixed reference pages. Add auction editor/review/cover UI in current workspace and mapping dropdowns. Preserve legacy generic-project rendering and seven other output types. Test migration up/down, ownership, mappings, conditional pages, QR decode, versioning and full API/browser flows; inspect generated PDF pages visually. No external AI required.
